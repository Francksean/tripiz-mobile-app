import 'package:flutter/material.dart';

/// Définit les poids de police utilisés dans l'application.
class FontWeights {
  /// Poids très fin (100).
  static const FontWeight thiny = FontWeight.w100;

  /// Poids normal/léger (300).
  static const FontWeight normal = FontWeight.w300;

  /// Poids gras (600).
  static const FontWeight bold = FontWeight.w600;
}
